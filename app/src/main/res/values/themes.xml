<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MusicPlayers" parent="Theme.MaterialComponents.DayNight.DarkActionBar">

        <item name="windowNoTitle">true</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary</item>
        <item name="colorOnPrimary">@color/primary</item>

        <item name="backgroundColor">@color/background1</item>
        <item name="android:windowBackground">@color/background1</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/background2</item>
        <item name="colorSecondaryVariant">@color/background2</item>
        <item name="colorOnSecondary">@color/background2</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->

        <item name="android:textColor">@color/white</item>
        <item name="android:textColorPrimary">@color/btn_ulang</item>
    </style>
</resources>